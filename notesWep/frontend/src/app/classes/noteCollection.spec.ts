import { Note_Collection } from './noteCollection';
// Test  for creating a collection
describe('Note_Collection', () => {
  it('should create an instance', () => {
    expect(new Note_Collection()).toBeTruthy();
  });
});
